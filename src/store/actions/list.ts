import { storeState } from '../store';
import { ThunkAction } from 'redux-thunk';
import { AnyAction } from 'redux';
import { TListItem } from '../../custom_types';

export const USER_UPDATE_DATA = 'USER_UPDATE_DATA';

